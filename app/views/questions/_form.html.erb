<%= form_for(@question) do |f| %>
  <p><%= f.label "Title" %></p>
  <p><%= f.text_field :title %></p>
  <p><%= f.label "Content" %></p>
  <p><%= f.text_area :content %></p>
  <p><%= f.submit "Submit" %></p>
<% end %>