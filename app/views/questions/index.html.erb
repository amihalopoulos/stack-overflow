  <ul class="all-questions">
    <% @questions.each do |question| %>
    <li class="question-link"> Votes: <%=question.votes.count %> | Answers: <%= question.answers.count %> <%= link_to question.title, question_path(question.id) %>
    </li>
    <% end %>
  </ul>


